# 2026.08.07.4 功能更新说明

## 登录与账号安全

- 登录页和注册页拆分为独立页面。
- 登录增加图形验证码校验。
- 同一 IP 和用户名连续登录失败 5 次后锁定 10 分钟。
- 所有登录用户均可修改自己的密码。
- 注册账号支持邀请码有效期，到期账号自动停止登录和访问。

## 登录态与邮箱服务

- Apple Account 登录态增加自动保活、失败重试和中文状态提示。
- 多个 Apple Account 登录态支持有限并发保活，避免单个账号阻塞全部任务。
- 登录态页面以表格展示账号、接口状态、子邮箱和保活结果。
- 取码登录支持 iCloud、Outlook/Exchange IMAP、Gmail 和自定义公网 IMAP 服务。
- 第三方 IMAP 支持自定义服务器、端口、用户名和应用密码。
- IMAP 自定义服务器增加内网地址访问防护。

## 隐私邮箱池

- 邮箱池支持组合搜索、账号筛选、状态筛选和分页。
- 支持勾选、当前页全选和批量操作。
- 支持批量设为可用、批量设为已使用、批量停用、批量导出和批量删除。
- 导出邮箱后记录“已导出”状态及最近导出时间。
- 每个邮箱增加“查看邮箱”，可查看已同步邮件的主题、发件人、时间和正文。
- 删除验证码邮件时同步清理远端邮件、本地记录和页面缓存。
- iCloud 邮件登录态失效时返回中文提示，不再显示原始服务器 HTML。

## 接口与数据

- 生成的邮箱 API 地址统一使用 `https://mail.sayt.cloud`。
- 用户可见时间统一为 `yyyy-MM-dd HH:mm`。
- 账号支持分类和标签，邮箱、登录态和所属用户关系展示更加清晰。
- 保留现有 `data/state.json` 数据格式，新版本可直接读取历史账号、登录态、邮箱和邮件记录。
