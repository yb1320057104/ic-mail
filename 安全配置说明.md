# 登录安全与审计配置

## 注册策略

- 第一个注册用户始终允许创建，并自动成为管理员，避免新部署无法初始化。
- `registration_enabled=false`：首个管理员创建后关闭新用户注册，这是默认和推荐配置。
- `registration_enabled=true` 且邀请码为空：开放注册。
- `registration_enabled=true` 且配置 `registration_invite_code`：注册必须提交管理员邀请码。
- 生产环境建议通过环境变量 `IPM_REGISTRATION_INVITE_CODE` 提供邀请码，不要提交到 Git。

## 登录限流

应用会同时统计来源 IP 和规范化用户名。任一维度触发限制都会返回 HTTP 429 和 `Retry-After`：

| 配置 | 默认值 | 说明 |
| --- | ---: | --- |
| `login_rate_limit_per_ip` | 5 | 单 IP 连续失败达到该次数后锁定 |
| `login_rate_limit_per_username` | 5 | 单用户名连续失败达到该次数后锁定 |
| `login_rate_limit_window_seconds` | 600 | 失败统计窗口，单位秒 |
| `login_backoff_max_seconds` | 600 | 锁定时间，单位秒 |
| `login_captcha_enabled` | true | 登录是否强制使用一次性图形验证码 |

连续输错密码 5 次后锁定 10 分钟。成功登录会清除该用户名的失败状态，但不会清空整个 IP 的失败历史。图形验证码五分钟有效，校验一次后立即失效。

服务只在请求直接来自本机回环地址时信任 `X-Forwarded-For` 和 `X-Real-IP`，因此生产环境应保持 Go 服务监听 `127.0.0.1`，由本机 Nginx 反向代理。

## Nginx 限速

可直接参考 `deploy/nginx-rate-limit.conf`。其中 `limit_req_zone` 必须放进 Nginx 主配置的 `http {}`，`server {}` 内容放进站点配置。修改后执行：

```bash
nginx -t && systemctl reload nginx
```

## 审计日志

以下操作会写入结构化 `security audit` 日志：

- 登录、注册、退出；
- 管理员接口；
- 所有会改变状态的 API；
- 完整运行数据导出；
- 在线更新。

日志包含事件、HTTP 方法、路径、状态码、结果、操作者 ID/用户名/角色、来源 IP 和耗时。日志不会记录密码、邀请码、Cookie、验证码或 API Key。

使用 systemd 部署时可查看：

```bash
journalctl -u icloud-privacy-mail -f | grep "security audit"
```
